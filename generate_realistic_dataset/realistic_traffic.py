#!/usr/bin/python3

'''
This program merges the web traffic with noise, so it can be used to test WF attacks
'''

def main():

    print("TODO")

    # input whit which noise to use, and for what 
    # {test, valid, train}%{twitch, streaming, etc. }

    #mergeDatasetNoise(result_dir, dataset, noise, offsets, test)
    #mergeDatasetNoise(result_dir, dataset, noise, offsets, valid)
    #mergeDatasetNoise(result_dir, dataset, noise, offsets, train)

if __name__=="__main__":
    main()